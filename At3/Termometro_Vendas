SELECT
    ID_VENDA,
    VALOR_LIQUIDO,
    ROUND(
        AVG(VALOR_LIQUIDO) OVER (),
        2
    ) AS MEDIA_GERAL,
    ROUND(
        VALOR_LIQUIDO - AVG(VALOR_LIQUIDO) OVER (),
        2
    ) AS DIFERENCA_MEDIA
FROM TB_VENDA
ORDER BY ID_VENDA;
